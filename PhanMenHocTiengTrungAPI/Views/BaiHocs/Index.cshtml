@model IEnumerable<PhanMenHocTiengTrungAPI.Models.BaiHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.chude)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idYoutube)
        </th>
      
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.chude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idYoutube)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            <a href="@Url.Action("Index","ChiTietBaiHocs",new {idBaiHoc = item.id})">Details</a>|
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
